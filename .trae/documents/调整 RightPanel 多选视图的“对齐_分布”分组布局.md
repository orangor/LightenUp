## 目标
- 将等距分布与固定间距分布归入“分布”分组，与“对齐”分组并列；删除现有“多选操作”分组，保持功能不变、布局更清晰。

## 具体调整
- 分布分组：
  - 第一行（两列）：横向等距、纵向等距（size="small"，block，统一禁用逻辑）。
  - 第二行：Space.Compact 包含“固定间距”InputNumber 与“应用横向/应用纵向”两个按钮。
- 对齐分组：
  - 第一行：参考对象选择 Select（selection/canvas/first，size="small"）。
  - 第二行：左/水平居中/右（三列）。
  - 第三行：上/垂直居中/下（三列）。
- 仅在 ActiveSelection 时显示上述两个分组；单选视图保持现状。

## 代码改动范围
- 文件：components/panels/RightPanel.tsx（仅重构多选视图的 JSX 排布与标题文案，不改逻辑与方法调用）。
- 保持现有禁用判断与调用 layerManager 的方法；不改样式文件与其它组件。

## 验证
- 构建通过；在窄侧栏下按钮不挤压，点击区域更友好。
- 等距/固定间距/对齐操作与参考对象选择工作正常，历史保存与渲染一致。