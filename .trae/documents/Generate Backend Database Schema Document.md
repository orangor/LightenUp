我将根据您的要求，编写一份**中文**的后端数据库建表需求文档。

文档将创建在：`e:\AiDome\momo\daydo\src\ai-image-editor\DATABASE_SCHEMA.md`

### 文档内容结构
1.  **概述**：V2 编辑器最小后端设计理念。
2.  **详细表结构定义**：
    *   **1️⃣ users（用户表）**：支持匿名/登录扩展。
    *   **2️⃣ projects（项目表）**：设计工程单元。
    *   **3️⃣ canvases（画布表）**：多尺寸与安全区支持。
    *   **4️⃣ nodes（元素表｜核心）**：单的一事实来源，包含 `props` 扩展设计。
    *   **5️⃣ groups（组合表）**：组合结构管理。
    *   **6️⃣ export_tasks（导出任务表）**：异步导出任务管理。
    *   **7️⃣ project_versions（版本快照 - 可选）**：历史记录支持。
3.  **核心设计原则与边界**：
    *   `nodes` 作为编辑器唯一状态源。
    *   `props` 字段的 JSON 扩展策略（承担 90% 扩展能力）。
    *   前后端职责分离说明（前端交互 vs 后端状态持久化）。

该文档将完全遵循您的“理性、不考虑感受”的要求，提供严谨的技术规范。